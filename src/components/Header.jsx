import DesktopNav from './DesktopNav'
function Header({ menuOpen, onMenuToggle, onNavigate }) { return <header className="header"><div className="header__inner section-wrap"><a className="brand" href="#inicio" onClick={onNavigate} aria-label="Ir al inicio">ML<span>.</span></a><DesktopNav menuOpen={menuOpen} onNavigate={onNavigate} /><a className="header__contact" href="#contacto">Contacto <span>↗</span></a><button className="menu-button" type="button" onClick={onMenuToggle} aria-label={menuOpen ? 'Cerrar menú' : 'Abrir menú'} aria-expanded={menuOpen}><span className={menuOpen ? 'open' : ''} /><span className={menuOpen ? 'open' : ''} /></button></div></header> }
export default Header
